<!doctype html>
<html>
    <head>
        <title>{{ page_title }} | {{ app_title }}</title>
        <script src="{{ url("../assets/jquery/jquery-3.3.1.min.js") }}"></script>
        <link rel="stylesheet" href="{{ url("../assets/semantic-ui/semantic.min.css") }}" />
        <script src="{{ url("../assets/semantic-ui/semantic.min.js") }}"></script>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
        {% block menu %}
            {% include 'Includes/Menu.twig' %}
        {% endblock %}
        {% block content %}
            Sobrescreva este bloco com o conteúdo de sua página
        {% endblock %}
    </body>
</html>